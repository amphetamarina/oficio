- [ ] @hermes id:${1:request-id}
  ${2:descreva o que o agente deve fazer.}
  ${0}

---

Template: hermes-request  
Use by pressing `Cmd/Ctrl+T` in Obsidian (requires core Templates plugin enabled).  
`${1}` / `${2}` / `${0}` let you jump between fields with Tab.

Para IDs baseados em data, use o atalho do plugin Templates para inserir a data atual como id (formato sugerido: `YYYYMMDDHHmmss`).
